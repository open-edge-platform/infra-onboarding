#!ipxe

echo "==== iPXE boot success, chainloading Micro-OS ==== "

set tink_url <TINK_STACK_URL>
echo sleeping for 3s
sleep 3
dhcp && chain ${tink_url}/discover.php?mac=${mac}&&uuid=${uuid}&&serial_id=${serial}&&en_ip=${ip}&&boot_url=${tink_url}
