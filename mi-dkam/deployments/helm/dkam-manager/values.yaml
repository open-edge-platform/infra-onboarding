# Default values for dkam-manager.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.

replicaCount: 1

image:
  repository: amr-registry.caas.intel.com/one-intel-edge/maestro-i/dkammgr
  pullPolicy: Always
  # Overrides the image tag whose default is the chart appVersion.
  tag: "latest"

service:
  servicePort: 5581  # port exposed to pods connecting to service
