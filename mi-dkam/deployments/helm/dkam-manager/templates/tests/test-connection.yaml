# SPDX-FileCopyrightText: (C) 2022 Intel Corporation
# SPDX-License-Identifier: LicenseRef-Intel
---
apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "mi-dkam.fullname" . }}-test-connection"
  labels:
    {{- include "mi-dkam.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "mi-dkam.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
